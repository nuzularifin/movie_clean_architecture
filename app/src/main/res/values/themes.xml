<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Movie" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="searchViewStyle">@style/Theme.Movie.MySearchView</item>

    </style>


    <style name="Theme.Movie.MySearchView" parent="Widget.AppCompat.Toolbar">
        <item name="editTextColor">#FFFFFF</item>
        <item name="searchIcon">@drawable/ic_search</item>
        <item name="closeIcon">@drawable/ic_menu</item>
        <item name="hintTextColor">@color/white</item>
        <item name="searchHintIcon">@drawable/ic_search</item>
    </style>

    <style name="SplashTheme" parent="Theme.Movie.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDisablePreview">true</item>
    </style>


    <style name="Theme.Movie.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="toolbarStyle">@style/ToolbarCustom</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:colorControlNormal">@color/white</item>
    </style>

    <style name="ToolbarCustom" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/black</item>
        <item name="searchIcon">@drawable/ic_search</item>
        <item name="searchHintIcon">@drawable/ic_search</item>
        <item name="hintTextColor">@color/white</item>
        <item name="defaultQueryHint">false</item>
        <item name="iconTint">@color/white</item>
        <item name="closeIconTint">@color/white</item>
        <item name="homeAsUpIndicator">@drawable/ic_back</item>
        <item name="searchViewStyle">@style/CustomSearchView</item>
    </style>

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="searchHintIcon">@null</item>
        <item name="iconifiedByDefault">false</item>
        <item name="android:iconifiedByDefault">false</item>
    </style>

    <style name="ThemeOverlay.Toolbar" parent="Theme.Movie">
        <!-- Text color -->
        <item name="android:searchIcon">@drawable/ic_search</item>
        <item name="android:editTextColor">#FF3232</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">#FF3232</item>
    </style>
</resources>